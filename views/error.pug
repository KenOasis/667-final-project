extends layout/main-layout.pug

block title
  title #{title}

block navbar
  case (isLoggedIn)
    when true
      include navbar/nav-logged-in
    default
      include navbar/nav-logged-out

block content
  h1 #{errorcode}

  script(type="text/javascript").
    let redirect_Page = () => {
      let timer = setTimeout(() => {
        fetch("/").
        then(() => {})
        .catch(error => console.log(error));
        window.clearTimeout(timer);
      }, 5000);
    }
    // not running ? cors ?
    redirect_Page();
    console.log("run");

  //- TODO, redirect to the original page or .....
  